{
  "name": "bsc-pos-functions",
  "description": "Cloud Functions backend for BSC POS -- currently just Card Intake's AI card reading (identifyCard). See ../CARD_AI_SETUP.md to deploy.",
  "private": true,
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^5.0.0"
  }
}
